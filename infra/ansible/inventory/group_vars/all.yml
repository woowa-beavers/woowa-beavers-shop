ansible_user: ec2-user
ansible_ssh_private_key_file: ~/.ssh/woowa-beavers-keypair.pem
ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

# 공통 변수
region: ap-northeast-2
project: woowa-beavers

# CloudWatch Agent 로그 설정
cw_log_group_prefix: /ec2
cw_log_stream: "{instance_id}"
